apiVersion: v1
kind: Service
metadata:
  name: qbittorrent-nodeport
  namespace: qbittorrent
spec:
  type: NodePort
  selector:
    app: qbittorrent
  ports:
    - name: http
      port: 30005
      targetPort: 30005
      nodePort: 30005
